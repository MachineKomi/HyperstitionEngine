import React from 'react';

const EntropyPool = () => {
    return (
        <div className="entropy-pool">
            <h2>Entropy Pool</h2>
            <p>Waiting for input...</p>
        </div>
    );
};

export default EntropyPool;
